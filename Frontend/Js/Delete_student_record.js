document.addEventListener("DOMContentLoaded",()=>{
    const urlParams = new URLSearchParams(window.location.search)
    const studentID = urlParams.get(studentID);
    

})

const delete_student_Record = (studentID)=>{

}